name: CI Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore ./VictorBaseDotNET.csproj

    - name: Build solution
      run: dotnet build ./VictorBaseDotNET.csproj --configuration Release /p:TreatWarningsAsErrors=false

    - name: Run tests
      run: dotnet test ./VictorBaseDotNET.csproj --no-build --configuration Release --verbosity normal
